contract BloodBank {

    mapping(address => bool) healty;
    address doctor;
    uint blood;

    constructor (uint amount) {
        this.doctor = doctor
    }

    function fb(uint amount) {
        1;
    }
}
